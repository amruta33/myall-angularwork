import { InputzoomDirective } from './inputzoom.directive';

describe('InputzoomDirective', () => {
  it('should create an instance', () => {
    const directive = new InputzoomDirective();
    expect(directive).toBeTruthy();
  });
});
